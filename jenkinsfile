@Library('shared_demo1') _

pipeline {
    agent any
    stages {
        stage ('Build') {
            steps {
                script {
                    welcome('Chinmai Gowda')
                    arithmetic.add(10,5)
                    arithmetic.mul(5,6)
                }
            }
        }
    }
}