pipeline {
    agent {
        label 'slave2'
    }

    stages {
        stage ('Checkout') {
            steps {
                script {
                    checkout([$class: 'GitSCM', 
                    branches: [[name: '*/master']],
                    userRemoteConfigs: [[url: 'https://github.com/chinmaigowda/mavendemo.git',
                    credentialsId: 'GitHub']]])
                }
            }
        }
        stage ('Build') {
            steps {
                sh '''
                    ls -lrt
                    sleep 5
                '''
            }
        }
        stage ('Test') {
            steps {
                sh '''
                    #!/bin/bash
                    ls -lrt
                    sleep 10
                '''
            }
        }
    }
}