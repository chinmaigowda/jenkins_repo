@Library('shared_demo1') _

pipeline {
    agent any
    stages {
        stage ('Build') {
            steps {
                sh 'set +x'
                script {
                    welcome('Chinmai Gowda')
                }
            }
        }
    }
}